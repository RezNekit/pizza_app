pizza_app.py is a main file 
static contains icons and ENTITY RELATIONSHIP DIAGRAM (ERD)
database_queries folder contains sql files for tables and data of them but they are NOT used. For convenience we sticked to ORM so:
        -models.py contains tables
        -seed_data.py seeds the data
        

margin is applied in controllers.py where views are created and some SQL code is writen
vat is applied in order_placement.py where we actually place the order
everything else is self-explanatory